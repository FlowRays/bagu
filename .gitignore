# 框架源码克隆（39MB，源码导读只引用 路径:行号，不入库）
rl/framework/

# 本地私密笔记：含公司名/面试轮次等个人信息，只在本地可见，不进 git
notes/interview.md

__pycache__/
.DS_Store
*.pyc

# 调试截图
*.png

# 付费题库汇总（sspoffer 会员内容），只在本地看，不进公开仓库
notes/oj-sspoffer-48.md

# 「想法思考」整本只在本地留明文，仓库里只有 private/*.enc 密文
ideas/
